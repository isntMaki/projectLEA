# 2026-09-22

## After the reboot: everything survived

The user's PC powered off mid-session. Verified on restart:

- The three source edits from 2026-09-21 late night are intact (`phaseHeartbeatInterval` x2 in
  `NetMatchSync`, the null-roster guard in `ClassManager`, the same-state early return in
  `GameManager.SetRemoteState`).
- `Tools/verify_scripts.py` still reports **All checks passed (129 assertions)**.
- Unity reopened the project and recompiled: `Editor.log` line ~20323, `CompileScripts: 5035ms`,
  `domain reloads=1`, `Asset File Changes: changed=12`. **Zero `error CS` lines anywhere after
  line 20000** — the only 105 error lines in the whole log are the stale block at lines
  87-709 / ~1515 that was already fixed on 2026-09-21. So the assembly is current and clean.
- Unity is running again (`Unity.exe` back up with the project open); the standalone client is
  NOT running.

## Still outstanding — the build is the blocker

`C:/Users/bib/OneDrive - bib & FHDW/Desktop/lea test/projectLEA.exe` is still the **Sep 17 21:04**
build. Every "second player gets nothing / no placeholders" symptom is that stale client. The
user has to rebuild from their interactive Editor; the agent shell cannot (batch mode hangs).

The client's `d3d12: Device failed error (887a0001)` plus the PC powering off both point at the
same cause: two fullscreen D3D12 processes on one 4 GB RTX 3050. Recommend the client run
**windowed**. This is not a game bug.

## Class data spot-check (Surge = Neon)

`Assets/Data/Manuel/Classes/Surge.asset` confirms the 1:1 renamed-agent design the user asked
for: Fast Lane / Relay Bolt / High Gear / Overdrive on C/Q/E/X with Overdrive as the ultimate -
Neon's kit, under a different name. All 29 class assets follow this shape (4 abilities, key
17/31/19/38, the 4th ultimate), and the scene roster is 29/29 with no unresolved GUIDs.
